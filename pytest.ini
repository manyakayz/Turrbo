[pytest]
markers =
    integration: requires the real C-MAPSS data/ folder and a trained .keras model (not present in CI; run locally after placing data/ and turbofan_rul_v4.keras)
addopts = -m "not integration"
